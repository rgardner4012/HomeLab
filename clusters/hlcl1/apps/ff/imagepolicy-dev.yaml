apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: ff-dev
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: ff
  filterTags:
    pattern: '^dev-[0-9]{14}$'
  policy:
    alphabetical:
      order: asc
